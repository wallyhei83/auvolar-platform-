{
  "buildCommand": "cd apps/portal && pnpm install && bash prisma-build.sh && pnpm build",
  "outputDirectory": "apps/portal/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "functions": {
    "apps/portal/src/app/api/**": {
      "maxDuration": 30
    }
  }
}